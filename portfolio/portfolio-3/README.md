# Portfolio Setup

## Installation

Run the following command to install Vite:

```
npm install vite
```

## Running the Project

After closing the terminal, use this command to run the development server:

```
npx vite
```

This will start Vite and allow you to preview.
